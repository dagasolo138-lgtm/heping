# 《生灵》项目交接总览（CODEX）

本文件用于防止项目记忆丢失，并帮助新的对话或开发者快速接手。

**维护规则：** 开发前按需查阅；每次有效开发后在末尾追加版本概述；每次影响 Pages 的发布同步更新根目录 `version.json`；修改已有文件前重新获取最新 SHA。

---

## 当前版本：v0.23.1

《生灵》是部署在 GitHub Pages 的纯前端 ES Module 动态世界模拟游戏。

### 不可破坏规则

- 世界事实优先；人物、传记、史书、关系与未来 AI 只读或解释事实。
- 人物不知玩家或游戏存在。
- 主观印象写入 `personal`；行动、建造、睡眠、播种、收获写入 `lifeEvents`。
- `globalThis.shengling` 为运行时模块挂接点；异步模块必须展开当前对象，避免覆盖其他挂接模块。
- 已有最小持久化；`sourceCommit` 发布前需要用实际发布 commit SHA 回填，不得保留占位值。

### 当前能力

- 十位村民、河谷地图、A* 寻路、采集、营地库存、草棚、储物棚、昼夜、天气、篝火、环境暴露。
- 最小持久化：世界时间、人物、地图、营地、规则、建筑、篝火、生态、道路、农田、食物储存、食物分配冷却、社会事件与史书可保存/读取。
- 效用评分：通用行动按生存、库存、季节、关系与候选收益评分，人物面板展示行动原因。
- 关系反馈：帮助意图与共同劳动会产生关系变化和每日去重记忆，覆盖普通劳动、建造、开田、播种、收获与运料。
- 冬季稀缺：季节影响浆果、木材、燃料消耗和储备目标。
- 营地规则：记录食物分配、篝火和储物访问规则；当前食物规则为 advisory/explanatory，除先到先得外尚不改变即时发放顺序。
- 事件传播：资源分配/拒绝、稀缺、关系变化、规则变化、建筑完成和重大收获会形成亲历/听闻；普通劳动只保留 lifeEvents。
- 规则史书：建筑、农田、资源拒绝、规则变化和重大社会事件写入聚落纪事。
- LLM 只读边界：仅导出 fact pack 供外部润色/问答，不接入行动决策，不允许写回世界状态。
- 资源恢复、踩踏路径/土路、食物批次与腐败、四季、粟米、两块农田、土壤肥力。
- 地图支持拖动、双指缩放、滚轮、键盘、Home 回营地与人物点选。
- 手机端默认收起地图状态；营地信息单列；人物列表横向滚动；构建信息在页面底部。
- 世界速度：0.5×、1×、2×、5×、10×，默认 1×。
- 营地满载时，携带物资的村民等待空位，避免无效搬运循环与继续叠加采集。
- 高倍速时路径采样随速度缩短，减少转弯路径被直线补全。
- 建造事实按实际建筑与材料写入，不再把储物棚记成草棚。

### 已知限制

- 当前世界存档仍是浏览器本地 `localStorage`，不是云存档；跨设备需要手动迁移浏览器数据。
- 食物分配规则除 `firstComeFirstServed` 外目前只参与解释、优先级展示和事件记录，尚未实现同 tick 排序队列。
- 浏览器实测、长时运行回归和线上 Pages 验证需在发布环境另行执行。

## 启动链

```text
index.html
  └─ src/app.js
       ├─ src/app-v4.js
       ├─ attachEcologyRuntime.js
       ├─ attachRoadRuntime.js
       ├─ attachSeasonRuntime.js
       ├─ attachFarmRuntime.js
       ├─ attachFarmExpansionRuntime.js
       ├─ attachFoodStorageRuntime.js
       ├─ attachWorldSpeedRuntime.js
       ├─ attachMapHudRuntime.js
       └─ attachBuildInfoRuntime.js
```

启动顺序：主世界 → 生态 → 路径 → 季节 → 农田 → 扩田 → 食物储存 → 世界速度 → 地图 HUD → 构建信息。

`attachFoodStorageRuntime` 只处理食物，不得覆盖阶段标题。Foundation 15 的标题、说明和倍速 UI 由 `attachWorldSpeedRuntime` 最后设置。

## 关键模块

| 文件 | 职责 |
|---|---|
| `src/app.js` | 启动器编排，不堆叠规则。 |
| `src/app-v4.js` | 创建核心世界、动作系统、地图 UI 并启动循环。 |
| `src/modules/time/worldSpeedSystem.js` | 倍率枚举、当前速度、速度事件。 |
| `src/bootstrap/attachWorldSpeedRuntime.js` | 倍速系统、按钮 UI、Foundation 15 文案、倍速样式。 |
| `src/styles/worldSpeed.css` | 倍速选择器桌面与移动端样式。 |
| `src/modules/actions/actionSystem.js` | 使用模拟 delta 推进世界。 |
| `src/modules/actions/actionPlanner.js` | 通用采集/搬运规划；携带物资时优先搬运。 |
| `src/modules/actions/storageGuard.js` | 从营地或快照计算可用容量，满载时阻止无效搬运。 |
| `src/modules/actions/constructionEffects.js` | 领料、运料、施工的事实记录，按实际建筑标签生成文本。 |
| `src/bootstrap/attachRoadRuntime.js` | 路径采样；采样间隔按当前世界倍率缩放。 |
| `src/ui/map/mapView.js` | 相机、拖动、双指缩放、键盘、人物点选。 |
| `src/bootstrap/attachMapHudRuntime.js` | 手机端地图状态收起/展开。 |
| `src/modules/farming/farmSystem.js` | 农田、播种、生长、收获、扩田、土壤结算。 |
| `src/modules/farming/soilModel.js` | 肥力、恢复、成长/产量倍率、收获消耗。 |
| `src/bootstrap/attachBuildInfoRuntime.js` | Pages 构建信息读数。 |

## 世界速度模型

允许倍率：`0.5× / 1× / 2× / 5× / 10×`。

基础速率 `WORLD_MINUTES_PER_REAL_SECOND = 6`：

| 倍率 | 世界分钟 / 现实秒 |
|---:|---:|
| 0.5× | 3 |
| 1× | 6 |
| 2× | 12 |
| 5× | 30 |
| 10× | 60 |

```text
realDelta = clamp((now - previous) / 1000, 0, 0.12)
simulationDelta = realDelta × 当前倍率
```

`simulationDelta` 推进世界时间、人物移动/工作、行动规划、人物需求、环境暴露、天气、篝火、季节、生态、作物、土壤和食物损耗。高倍速不只是时钟加速。

`simulation:speed` 事件：

```text
{ speed: { value, label, worldMinutesPerRealSecond }, previous, reason, time }
```

运行时 API：

```js
window.shengling.worldSpeedSystem.get()
window.shengling.worldSpeedSystem.set(5)
window.shengling.worldSpeedRuntime.set(2)
```

速度不持久化；刷新恢复 1×；暂无暂停 0×。

### 高倍速路径规则

路径采样基准为 240ms 现实时间：

```text
sampleInterval = max(16ms, 240ms / 当前倍率)
```

这使 10× 下单次采样的移动距离与 1× 接近，降低折线路径被直线补全的概率。

## 库存与建造防护

- `availableCampStorage(camp, storage)` 优先读取 `storage.available`；没有快照时用 `capacity - items 总量` 推导。
- 人物携带物资时，若可用容量为 0，`planNextAction()` 返回空任务，人物等待容量释放。
- 营地因进食、施工领料或食物腐败释放容量后，后续规划自动恢复搬运。
- 建造领料、运输、施工文本必须从 `buildingSystem.get(siteId).label` 获取实际建筑名称。

## 农业关键参数

- 一年 360 天；春夏秋冬各 90 天。
- 粟米：春可播，夏 ×1.25，秋 ×0.62，冬 ×0；成熟全年可收。
- 基础收获 8，返种 2，收获肥力 -18。
- 土壤：初始 78；休耕每世界分钟 +0.0015；成长倍率 1.06/0.96/0.78/0.60，产量倍率 1.12/1.00/0.80/0.62。
- 第二块田由第一块田首次收获解锁，开垦需要 10 工作量。

## 移动端规则

- `map-canvas-wrap.is-hud-collapsed` 时只展示时间和天气。
- 点击“地图信息”才展开季节、生态、路径、农事、扩田、食物信息。
- `.settlement-strip` 移动端必须单列。
- 倍速选择器在地图画布外，始终可见，五个按钮同一行均分。
- 构建信息不得使用 `position: fixed`。

## Pages 验证

`version.json` 必须记录 `version`、`buildId`、`sourceCommit`、`branch`。页面用 `cache: no-store` 加 `cacheBust` 读取清单，并在页面底部显示构建读数。

```text
https://dagasolo138-lgtm.github.io/heping/version.json?cacheBust=<任意新数字>
```

页面底部与清单的 `buildId`、`sourceCommit` 一致，即可确认线上读取目标构建。

## 已知限制

1. 存档为浏览器本地 `localStorage`，不是云存档；发布后仍需用实际 commit SHA 回填 `version.json.sourceCommit` 并验证 Pages 读数。
2. 食物分配规则除 `firstComeFirstServed` 外当前为 advisory/explanatory，尚未真正改变即时请求的发放顺序。
3. 草棚、储物棚、农田仍未作为寻路障碍。
4. 农业只有两块田和一种粟米。
5. 扩田使用固定候选点，没有玩家点选、地形评分或劳动力预算。
6. 肥力、天气、季节、食物损耗和高倍速参数尚未平衡测试。
7. 10× 会更快产生日志和状态刷新；暂无日志聚合或后台跳帧优化。
8. 当前环境未进行浏览器自动化回归；浏览器实测需发布前另行执行。

## 下一阶段建议

1. 施肥/堆肥与休耕策略。
2. 更多作物：不同播种季、耐寒性、肥力消耗、成长周期和产量。
3. 灌溉、虫害、洪涝、干旱与粮食储备。

## 版本更新记录（只追加）

### v0.1 · 人物系统基础
- 十位村民、状态、技能、关系、库存、人生事实和个人记忆。

### v0.2 · 起始河谷地图
- 160m × 120m、河流、森林、石滩、营地与资源物件。

### v0.3 · 自主行动循环
- 取水、采集、砍树、搬运、休息、A* 寻路和营地库存。

### v0.4 · 建造与分工
- 草棚工地、运料、施工、居住分配和建造 UI。

### v0.4.1 · 项目交接文档
- 新建 `codex.md`。

### v0.4.2 · 交接规则调整
- 开发前按需查阅 `codex.md`；每次有效开发后追加版本概述。

### v0.5 · 昼夜、睡眠与居住效果
- 昼夜光照、夜间睡眠、露宿和草棚恢复效果。

### v0.6 · 天气、篝火与环境暴露
- 天气、降雨、温度、篝火、添柴、潮湿与受寒。

### v0.7 · 储物棚与营地容量
- 初始露天容量、储物棚容量与保护效果。

### v0.8 · 自然资源恢复
- 树木和浆果恢复队列；资源耗尽标记与占用延后恢复。

### v0.9 · 聚落路径
- 踩踏路径、土路、移动速度加成。

### v0.10 · 第一块粟田
- 粟米、农田、开垦、播种、收获与稳定食物来源。

### v0.11 · 食物批次、储存损耗与天气保护
- 食物批次、新鲜度、腐败、储物保护。

### v0.12 · 农业深化与季节
- 四季、温度趋势、春播与季节成长倍率。

### v0.13 · 第二块人工扩田
- 首次收获解锁第二块田，复用农业与食物链路。

### v0.13.1 · 可验证 GitHub Pages 部署
- `version.json`、构建读数、部署清单验证。

### v0.14 · 地图交互优化与土壤肥力
- 双指缩放、键盘导航、移动端卡片布局、土壤肥力与肥力可视化。

### v0.14.1 · 移动端界面修复
- 手机端地图状态默认收起；营地信息恢复单列；构建信息移入文档流；食物模块不再覆盖阶段文案。

### v0.15 · 世界速度
- 世界速度支持 0.5×、1×、2×、5×、10×；完整模拟 delta 同步推进时间、行动、需求与环境。

### v0.15.1 · 全面审计修复
- 路径采样间隔按世界速度缩放，降低 10× 下转弯路线误判。
- 营地满载时阻止无效搬运循环与继续采集叠加；容量释放后自动恢复搬运。
- 建造、领料和施工日志改为按实际建筑/材料记录。
- 完成启动、速度、事件、库存、农业、食物、建造、环境与移动端静态审计；全量浏览器回归和 CI 结果仍不可用。

### v0.16.0 · 最小持久化底座
- 新增世界存档系统，提供 `window.shengling.worldSaveSystem.exportSnapshot/save/load/clear/hasSave/getMeta` DevTools API。
- `gameTime`、营地、建筑、农田、篝火、生态、道路、食物储存补充 `exportState/importState`，人物与地图继续复用既有导出导入。
- 存档第一版使用 `localStorage` 手动槽位，读取时会暂停行动循环、导入长期世界事实、重建行动运行时人物位置后继续模拟。

### v0.16.1 · 存档入口与自动保存
- 地图面板新增世界存档控制条，支持保存、读取、重置和自动保存开关。
- 自动保存写入 `autosave` 槽位：开启后每 60 秒现实时间尝试保存一次，跨世界日也会保存；手动保存继续写入 `manual` 槽位。
- 读取按钮会自动选择最新的手动/自动存档；重置会清除两个槽位并刷新新世界。

### v0.17.0 · 效用竞价行动底座
- 新增行动欲望、稀缺度、社会效用占位、候选行动和效用评分模块。
- `planNextAction()` 在保留携带物资、极端饥渴和旧 planner 回退的前提下，先为取水、采集、砍树、休息生成候选并按效用分选择。
- 被效用 planner 选中的任务会携带 `data.utility` 调试摘要，为下一轮人物面板评分展示做准备。

### v0.17.1 · 行动评分调试面板
- 人物当前行动视图保留 `utility` 调试信息，人物详情面板新增“行动原因”卡片。
- 效用 planner 选中的取水、采集、砍树、休息会显示总分、主要因素和候选评分；搬运和紧急生存行为也会给出结构化原因。
- 取暖任务写入受寒/潮湿评分摘要，为天气压力下的行动解释做准备。

### v0.18.0 · 关系反哺行动评分
- 社会效用开始读取亲属、伴侣、信任和好感关系：亲近者饥渴较高时，取水和采集会得到额外评分。
- 关系紧张者位于候选行动目标附近时，行动评分会受到避让惩罚。
- 通用劳动完成时，若亲近者正在附近执行同类工作，会小幅提升双方熟悉、好感和信任，为共同劳动关系闭环打底。

### v0.18.1 · 关系事件与共同记忆
- 社会效用评分会把被帮助对象写入 `utility.socialTargets`，行动完成后形成帮助意图记忆和 `social:relation-changed` 事件。
- 共同劳动关系反馈现在会写入双方 `personal` 记忆，并按每日/行动去重，避免同一天同类劳动反复刷记忆。
- 关系反馈事件包含 actor、target、action、delta、source 和 time，为后续事件传播系统提供结构化输入。

### v0.19.0 · 冬季稀缺参数
- 生态恢复加入季节倍率：浆果和树木在冬季恢复显著变慢，恢复队列记录 depletion 时的季节和恢复时长。
- 冬季采集浆果产量降为 25%，秋季降为 65%，行动任务会记录季节产量倍率。
- 冬季食物与木材目标提高，效用 planner 会在冬季更重视粮食和燃料储备；冬季寒冷/冷雨下篝火燃料消耗加快。

### v0.19.1 · 食物分配与稀缺事件
- 新增食物/饮水分配系统，当前规则为 `firstComeFirstServed`，保留后续 campRules 接管入口。
- 饥渴结算改为通过分配系统请求水和食物，成功时发出 `survival:resource-distributed`，失败时按冷却发出 `survival:resource-denied` 与 `survival:scarcity`。
- 食物不足不再静默失败；分配失败会保留饥饿并增加少量压力，让冬季储备不足更容易自然形成危机。

### v0.20.0 · 营地规则底座
- 新增 `campRulesSystem`，把食物分配、篝火使用和储物访问的营地规则记录为可导出、可导入的数据。
- 食物/饮水分配会读取当前食物分配规则，并在分配/拒绝事件中写入规则、优先分和解释，为后续平均分配、劳动者优先、健康低者优先、亲属优先和地位优先提供入口。
- 世界存档纳入营地规则快照，读取存档时会恢复规则修订、历史和当前规则，避免刷新后营地习俗丢失。

### v0.21.0 · 事件传播与共同记忆
- 新增纯规则 `socialEventSystem`：行动完成、分配成功/拒绝、关系变化和规则变化会形成结构化社会事件。
- 同地点附近人物会获得“亲眼所见”个人记忆；见证者会按关系、社交技能和事件严重度把事件传播为“听闻”记忆，且使用稳定哈希概率保证可复现。
- 社会事件和传播记录纳入世界存档；人物详情新增“亲历与听闻”列表，区分亲历事件和传闻。


### v0.22.0 · 规则史书系统
- 新增纯规则 `chronicleSystem`：建筑完成、农田成熟/收获、食物分配拒绝、营地规则变化和重大社会事件会被收集为结构化史实。
- 每十个世界日自动生成一条锁定的聚落纪事；建筑完成、资源拒绝和规则变化会生成即时大事记，史书条目只基于已记录事实。
- 聚落纪事纳入世界存档并在页面中展示，刷新读取后保留既有史书，不使用 LLM 生成或改写世界事实。


### v0.23.0 · LLM 只读解释层边界
- 新增 `llmBoundary`、`factPackBuilder` 与 `aiPolicy`，只导出人物传记、锁定史书润色和人物已知记忆问答所需的 fact pack。
- fact pack 明确禁止编造事实、改写世界状态或决定行动；提示词导出只面向外部 LLM 润色/解释，不接入决策层。
- 输出校验要求 `usedFactIds` 必须来自 fact pack，且拒绝 `statePatch`、`worldPatch`、`nextAction` 等越界写入字段。


### v0.23.1 · 稳定性修复
- 修正版本文档与部署清单到 v0.23.1，并说明发布清单的 `sourceCommit` 需要使用实际发布 commit SHA。
- 世界存档纳入食物分配系统状态，读取旧存档缺少该字段时安全跳过，读取新存档时保留资源拒绝冷却。
- 关系反馈统一到行动完成流程，建造、开田、播种、收获和运料也能触发帮助意图/共同劳动反馈；睡眠、取暖和添柴不刷共同劳动。
- 社会事件传播降噪：普通劳动只保留 lifeEvents，不再默认进入亲历/听闻；仅传播资源、规则、关系、建筑完成和重大收获等社会意义事件。
- 营地食物规则明确标注 advisory/explanatory 状态，资源分配/拒绝事件记录规则是否实际参与排序，避免 UI 与事件误导。
